/// <reference path="../Root/Root.ts" />

class SimObject {
    // Properties
    root: Root;

    // Constuct / Destruct
    constructor(newRoot: Root) {
        this.root = newRoot;
    }

    // Methods
    update() {
    }
}